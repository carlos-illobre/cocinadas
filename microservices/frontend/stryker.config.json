{
  "$schema": "./node_modules/@stryker-mutator/core/schema/stryker-schema.json",
  "_comment": "Mutation testing (docs/TESTING.md). Informa, no reprueba: existen mutantes equivalentes que ninguna prueba puede matar, y un umbral obligaría a pelear con eso en vez de leer los sobrevivientes que importan. El objetivo es que no quede ningún sobreviviente sin explicar.",
  "plugins": ["@stryker-mutator/vitest-runner"],
  "testRunner": "vitest",
  "vitest": {
    "configFile": "vite.config.ts"
  },
  "mutate": ["src/**/*.{ts,tsx}", "!src/main.tsx", "!src/pruebas/**", "!src/**/*.test.{ts,tsx}"],
  "reporters": ["clear-text", "progress", "html", "json"],
  "htmlReporter": { "fileName": "reports/mutation/index.html" },
  "jsonReporter": { "fileName": "reports/mutation/mutation.json" },
  "thresholds": { "high": 90, "low": 70, "break": null },
  "coverageAnalysis": "perTest",
  "tempDirName": ".stryker-tmp"
}
