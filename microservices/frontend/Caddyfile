# Sirve la SPA con el catálogo adentro (ADR-013, ADR-015). Es un inquilino: escucha en
# HTTP y no sabe por qué dominio lo llamaron ni termina TLS. De eso se ocupa el proxy de
# la máquina (ADR-016, infrastructure/proxy/), o lo que sea que haya adelante.
#
# `:80` sin nombre de sitio es a propósito: contesta a cualquier Host. Quién es el dueño
# de cada dominio ya lo decidió el proxy antes de llegar acá, y así la misma imagen sirve
# igual en localhost, detrás del proxy propio o detrás del de otra aplicación.
#
# Lo que sí vive acá y no en el proxy: las cabeceras de seguridad, la política de
# contenido, el ruteo de la SPA y el caché. Son de la aplicación, y se despliegan con el
# código que las necesita.
{
	# Sin TLS: no emite ni guarda certificados, así que tampoco necesita volúmenes.
	auto_https off
	# La API de administración deja reconfigurar el servidor entero por HTTP. No hace
	# falta: la configuración cambia recreando el contenedor.
	admin off
	log {
		output stdout
		format console
		level {$LOG_LEVEL}
	}
}

# Sitio interno para el healthcheck del compose y del despliegue. No se publica ningún
# puerto hacia acá: solo se llega desde adentro del contenedor.
:8081 {
	respond /salud 200
}

:80 {
	root * /srv
	encode zstd gzip

	# Cabeceras de seguridad, para todo lo que sale de acá.
	header {
		# Un año de HTTPS obligatorio. Sobre http:// el navegador la ignora, así que en
		# desarrollo no molesta.
		Strict-Transport-Security "max-age=31536000; includeSubDomains"
		# Sin adivinar el tipo: una foto servida como imagen no se ejecuta como script.
		X-Content-Type-Options "nosniff"
		# Al salir del sitio se manda el origen, nunca la ruta completa.
		Referrer-Policy "strict-origin-when-cross-origin"
		# Nada de cámara, micrófono ni ubicación: la app no los usa.
		Permissions-Policy "camera=(), microphone=(), geolocation=(), payment=(), usb=()"
		# De dónde puede cargar cada cosa. Las tipografías vienen de Google Fonts; el resto
		# es propio. `unsafe-inline` en los estilos es por los `style` que pone React para
		# las barras y el gantt; los scripts no lo tienen.
		Content-Security-Policy "default-src 'self'; script-src 'self'; style-src 'self' 'unsafe-inline' https://fonts.googleapis.com; font-src 'self' https://fonts.gstatic.com; img-src 'self' data:; connect-src 'self'; media-src 'self'; object-src 'none'; base-uri 'self'; form-action 'self'; frame-ancestors 'none'"
		# El servidor no anuncia qué es.
		-Server
	}

	# Las rutas del navegador (/receta/..., /cocinando/...) no existen como archivo: se
	# devuelve index.html y la SPA las resuelve. El catálogo queda afuera: pedir un JSON
	# que no existe tiene que dar 404 y no el index.html, que api.ts leería como receta.
	@spa not path /api/*
	handle @spa {
		try_files {path} /index.html
	}

	file_server

	# Los assets llevan hash en el nombre: se pueden cachear sin límite. index.html no.
	@assets path /assets/*
	header @assets Cache-Control "public, max-age=31536000, immutable"
	header /index.html Cache-Control "no-cache"

	# Las fotos del catálogo cambian solo con un despliegue nuevo (viajan en la imagen,
	# ADR-006): un día de caché evita bajar 25 fotos cada vez que se abre una receta.
	@fotos path /api/catalogo/fotos/*
	header @fotos Cache-Control "public, max-age=86400"
}
