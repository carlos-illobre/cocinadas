# Configuración real: nunca al repositorio (ver docs/SECURITY.md)
.env
deployment/*/.env

# Dependencias y artefactos de build de cada servicio
node_modules/
dist/
# El catálogo se genera de data/ en cada build (ADR-015): la fuente es data/, no esto.
microservices/frontend/public/api/
coverage/
reports/
.stryker-tmp/
*.tsbuildinfo

# Sistema operativo y editores
.DS_Store
Thumbs.db
desktop.ini
.vscode/
.idea/

# Temporales de la sesión de Claude
.claude/settings.local.json

# Bytecode de Python (deploy.py se compila al verificarlo)
__pycache__/
*.pyc
